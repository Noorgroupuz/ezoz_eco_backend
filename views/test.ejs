<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <button>Click</button>
    <!-- <div id="editor">salom</div> -->
    <!-- <script src="https://cdn.ckeditor.com/ckeditor5/35.0.1/classic/ckeditor.js"></script> -->
    <script src="https://cdn.ckeditor.com/ckeditor5/35.0.1/decoupled-document/ckeditor.js"></script>
    <!-- <script>
      let editor;
      ClassicEditor.create(document.querySelector("#editor"))
        .then((res) => {
          editor = res;
        })
        .catch((error) => {
          console.error(error);
        });
      document.querySelector("button").addEventListener("click", () => {
        // console.log(document.querySelector("#editor"));
        console.log(editor.setData());
      });
    </script> -->
    <h1>Document editor</h1>

    <!-- The toolbar will be rendered in this container. -->
    <div id="toolbar-container"></div>

    <!-- This container will become the editable. -->
    <div id="editor" class="ck-focused" style="border: 1px solid black"></div>
    <script>
      let edit;
      DecoupledEditor.create(document.querySelector("#editor"))
        .then((editor) => {
          edit = editor;
          const toolbarContainer = document.querySelector("#toolbar-container");
          toolbarContainer.appendChild(editor.ui.view.toolbar.element);
        })
        .catch((error) => {
          console.error(error);
        });
      document.querySelector("button").addEventListener("click", () => {
        // console.log(document.querySelector("#editor"));
        console.log(edit.setData("salom"));
      });
    </script>
    <script></script>
  </body>
</html>
